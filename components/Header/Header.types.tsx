import * as React from 'react'
import { PositionProps, LayoutProps, SpaceProps } from 'styled-system'

export type HeaderProps = React.HTMLProps<HTMLElement> & PositionProps & LayoutProps & SpaceProps
